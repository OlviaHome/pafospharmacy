# Optional. When both values are absent, the app uses clearly marked fixtures.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Optional canonical origin for absolute metadata URLs.
SITE_URL=http://localhost:3000
